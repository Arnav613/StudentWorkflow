# Python 3.14. Note the pydantic floor: 2.11 and earlier ship no cp314 wheel,
# so pip falls back to compiling pydantic-core from Rust source and fails.
fastapi>=0.118.0
uvicorn[standard]>=0.37.0
pydantic>=2.12
pydantic-settings>=2.11.0
pyjwt>=2.10.1
httpx>=0.28.1
python-dotenv>=1.1.1
# Fernet, for the refresh tokens in google_tokens. See app/core/crypto.py.
cryptography>=44.0
# Page selection for the documents sent to the model. Pure Python on purpose:
# a wheel that has to compile is a deploy that can fail on a free dyno.
pypdf>=5.1
